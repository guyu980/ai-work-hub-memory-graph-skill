interface:
  display_name: "AI Work Hub Memory Graph"
  short_description: "Validated cross-project investment memory"
  default_prompt: "Use $ai-work-hub-memory-graph to build a bounded context pack, connect this input to prior projects and theses, update project state and compressed cards, queue review-only graph changes, rebuild generated indexes, and validate the private Memory Graph."
