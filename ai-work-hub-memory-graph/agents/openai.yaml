interface:
  display_name: "AI Work Hub Memory Graph"
  short_description: "Persist and analyze reusable investment sources"
  default_prompt: "Use $ai-work-hub-memory-graph to persist and substantively analyze this reusable source, connect it to prior investment memory, and write back only durable increments."
